<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.lltest.appFrameTest"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- WebView needs INTERNET permission -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name="com.lltest.util.NetworkStatusReceiver" >

            <intent-filter android:priority="100" >
                <action android:name="android.net.wifi.STATE_CHANGE" />
                <action android:name="android.net.wifi.WIFI_STATE_CHANGED" />
                <action android:name="android.net.wifi.WifiManager.RSSI_CHANGED_ACTION"/>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
                <action android:name="android.net.wifi.WifiManager.WIFI_STATE_CHANGED_ACTION" />
            </intent-filter>

        </receiver>

        <activity android:name="com.lltest.appFrameTest.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>


        <activity android:name="com.lltest.appFrameTest.Activity2">

        </activity>


        <activity android:name="com.lltest.appFrameTest.Activity3">

        </activity>

        <!-- need to test excludeFromRecents -->
        <activity
            android:name="com.lltest.appFrameTest.Activity4"
            android:screenOrientation="portrait"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="ecomm.stg.mpay.samsung.com"
                    android:pathPrefix="/ew/v1/vco/checkout" />
                <data
                    android:scheme="https"
                    android:host="ecomm.mpay.samsung.com"
                    android:pathPrefix="/ew/v1/vco/checkout" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="lltest"
                    android:host="lltest" />
            </intent-filter>

            <!-- Accept chrome links -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="lltest"
                    android:host="lltest"
                    android:pathPrefix="/" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="lltest"
                    android:pathPrefix="/" />
            </intent-filter>

        </activity>


        <activity
            android:name="com.lltest.appFrameTest.ExitActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:autoRemoveFromRecents="true"/>

    </application>

</manifest>